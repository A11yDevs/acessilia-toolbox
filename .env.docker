# Provider endpoints — Docker variant.
# Uses container names on the shared network instead of localhost.
# Symlinked or copied to .env when running inside Docker.

DOCLING_SERVE_URL=http://docling-serve:5001
DOCLING_SERVE_IMAGE=ghcr.io/docling-project/docling-serve-cpu:v1.32.0
DOCLING_SERVE_PORT=5001
DOCLING_SERVE_ENABLE_UI=false

TOOLBOX_PROVIDERS_CONFIG=providers-config.yaml

MINIO_URL=http://acessilia-minio:9000
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ACCESS_KEY=acessiliaadmin
MINIO_SECRET_KEY=sMvhTvcQWfZry72Zz7lDuVL7D5NUsQK

VALKEY_URL=redis://acessilia-valkey:6379
VALKEY_PORT=6379

TOOLBOX_HOST=0.0.0.0
TOOLBOX_PORT=8002