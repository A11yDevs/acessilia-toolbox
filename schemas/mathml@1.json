{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:a11y-devs:schema:mathml:1.0.0",
  "title": "MathML Expression",
  "description": "A mathematical expression represented in MathML notation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mathml"
  ],
  "properties": {
    "mathml": {
      "type": "string",
      "minLength": 1,
      "description": "MathML markup of the mathematical expression."
    },
    "latex": {
      "type": "string",
      "description": "Original LaTeX source, when converted from LaTeX."
    },
    "direction": {
      "type": "string",
      "enum": ["latex-to-mathml", "mathml-to-latex"],
      "description": "Conversion direction."
    }
  }
}