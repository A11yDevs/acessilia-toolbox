{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:a11y-devs:schema:latex:1.0.0",
  "title": "LaTeX Expression",
  "description": "A mathematical expression represented in LaTeX notation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "latex"
  ],
  "properties": {
    "latex": {
      "type": "string",
      "minLength": 1,
      "description": "LaTeX representation of the mathematical expression."
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Recognition confidence score, when applicable."
    },
    "page": {
      "type": "integer",
      "minimum": 1,
      "description": "Source page number, when extracted from a document."
    },
    "bbox": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 4,
      "maxItems": 4,
      "description": "Bounding box [left, top, right, bottom] in page coordinates."
    }
  }
}